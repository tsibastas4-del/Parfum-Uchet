# Інструкція: Підключення Supabase

## 📋 Крок 1: Реєстрація/Вхід
- Зайдіть на [supabase.com](https://supabase.com) і натисніть "Start your project".
- Увійдіть через GitHub (або зареєструйтесь).

## 🚀 Крок 2: Створення Проекту
- Натисніть **"New Project"**.
- Name: `PerfumeCRM` (або як хочете).
- Password: придумайте складний пароль (він нам зараз не знадобиться, але збережіть).
- Region: `Frankfurt` (найближчий) або будь-який.
- Натисніть **"Create new project"**. Зачекайте пару хвилин.

## 🔑 Крок 3: Отримання Ключів
- Коли проект створиться, ви побачите сторінку налаштувань.
- Знайдіть розділ **API Settings** (іконка шестерні -> API).
- Вам треба два значення:
    - `Project URL` (виглядає як `https://xyz...supabase.co`)
    - `Project API Key` (виглядає як довгий набір букв `eyJ...`, позначений як `anon` `public`).
- **⚠️ Не копіюйте `service_role` ключ!** Тільки `anon`.

## 📊 Крок 4: Налаштування Таблиці
- Зліва в меню оберіть **Table Editor** (іконка таблиці).
- Натисніть **"New Table"**.
- Name: `app_data`
- **ВАЖЛИВО:** Вимкніть галочку **"Enable Row Level Security (RLS)"** 
  - (Для простоти, щоб працювало одразу. Пізніше можна налаштувати)
- В колонках:
    - `id` (залиште int8, Primary Key) - воно дефолтне.
    - Натисніть **"Add Column"**:
        - Name: `json_data`
        - Type: `jsonb`
    - *(Опційно)* Натисніть **"Add Column"** ще раз:
        - Name: `updated_at`
        - Type: `timestamp`
        - Default Value: `now()` (опційно)
        - **Примітка:** Ця колонка не обов'язкова, система працює і без неї
- Натисніть **"Save"**.

## 🔌 Крок 5: Підключення в Програмі
- Відкрийте нашу CRM.
- Натисніть ⚙️ (Налаштування) вгорі праворуч.
- Вставте **URL** та **Key**.
- Натисніть **"Перевірити з'єднання"**.
- Якщо зелене — натисніть **"Зберегти в хмару"**.

---

## 🛠️ Вирішення Проблем

### Помилка: "Таблиця app_data не існує"
**Рішення:** Створіть таблицю згідно з Кроком 4.

### Помилка: "Permission denied" або "RLS"
**Рішення:** 
1. Перейдіть в Supabase Dashboard → Authentication → Policies
2. Знайдіть таблицю `app_data`
3. Вимкніть RLS (кнопка "Disable RLS")

### Помилка: "Невірний URL або Key"
**Рішення:** Перевірте, що ви скопіювали правильні значення:
- URL має починатися з `https://` і закінчуватися на `.supabase.co`
- Key має бути довгим (200+ символів) і починатися з `eyJ`

### Помилка: "Timeout"
**Рішення:** 
- Перевірте інтернет-з'єднання
- Спробуйте ще раз через хвилину
- Можливо, сервер Supabase тимчасово недоступний

---

## ✅ Що Нового в Останній Версії

- ✨ Детальні повідомлення про помилки з інструкціями
- 📊 Перевірка розміру даних перед відправкою (макс 10 MB)
- ⏱️ Відображення часу останньої синхронізації
- 🔍 Покращене логування для дебагу
- ⚡ Збільшений таймаут до 20 секунд
